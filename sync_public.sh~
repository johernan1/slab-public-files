#!/bin/bash
# sync_public.sh
# Sincroniza automáticamente los ficheros públicos con el repo público

# Configura rutas
REPO_PUBLIC="public-export"
SRC_ACAD="acad"
SRC_MODELOS="test/modelos"

# Copiar ficheros al repo público
cp "$SRC_ACAD/260107DibujoConMacro.dwg" "$REPO_PUBLIC/"
cp "$SRC_ACAD/slab.cuix" "$REPO_PUBLIC/"
cp "$SRC_MODELOS"/*.json "$REPO_PUBLIC/"

# Entrar en la worktree pública
cd "$REPO_PUBLIC" || exit 1

# Comprobar si hay cambios
#if ! git diff-index --quiet HEAD --; then
#    git add .
#    git commit -m "Sync public files"
#    git push
#    echo "Ficheros públicos sincronizados ✅"
#else
#    echo "No hay cambios en los ficheros públicos"
#fi
